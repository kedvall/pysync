[project]
name = "pysync-test"
version = "0.1.0"
description = ""
readme = ""
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31,<3",
]

[dependency-groups]
dev = [
    "mypy>=1.15,<2",
    "ruff>=0.9.7",
]
